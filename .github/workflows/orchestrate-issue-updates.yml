# Provides downstream workflows the appropriate context for updating projects
# when an issue changes
#
# This workflow:
# - Determines which Product the issue belongs to
# - Routes the issue to the correct reusable workflow
# - Calls sync-project.yml and supplies it with the necessary project context
#
# This workflow is automatically triggered when:
# - Issue body is edited
# - Labels are added
# - Labels are removed

name: Orchestrate Issue Updates

on:
  issues:
    types:
      - edited
      - labeled
      - unlabeled

# Prevent multiple runs for the same issue.
concurrency:
  group: sync-project-${{ github.event.issue.id }}
  cancel-in-progress: true

permissions:
  contents: read
  issues: read

jobs:
  initialize:
    runs-on: ubuntu-latest

    outputs:
      product: ${{ steps.product.outputs.product }}

    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.ACTIONS_APP_ID }}
          private-key: ${{ secrets.ACTIONS_APP_PRIVATE_KEY }}

      - name: Read Product
        id: product
        uses: actions/github-script@v8
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const response = await github.request(
              "GET /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values",
              {
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                headers: {
                  "X-GitHub-Api-Version": "2022-11-28"
                }
              }
            );

            const product = response.data.find(
              field => field.issue_field_name === "Product"
            )?.single_select_option?.name ?? "";

            core.setOutput("product", product);

  sync-vetway:
    needs: initialize
    if: needs.initialize.outputs.product == 'VetWay'
    uses: ./.github/workflows/sync-project.yml
    secrets: inherit
    with:
      project-owner: Angel-Organization
      project-owner-type: organization
      project-number: 1
      project-id: PVT_kwDOElfYpM4BeD2H
      config: |
        {
          "fields": {
            "Status": {
              "id": "PVTSSF_lADOElfYpM4BeD2HzhYg1gE",
              "type": "singleSelect",
              "options": {
                "Blocked": "11a9b027",
                "Scoping": "6695b6f2",
                "Todo": "f75ad846",
                "In Development": "47fc9ee4",
                "QA Testing": "4b2dec1a",
                "QA Passed": "9679dba4",
                "Test": "9c54861d",
                "Stage": "7e9333bf",
                "Production": "c93ec73b"
              }
            }
          }
        }

  sync-scriptsure:
    needs: initialize
    if: needs.initialize.outputs.product == 'ScriptSure'
    uses: ./.github/workflows/sync-project.yml
    secrets: inherit
    with:
      project-owner: daw-systems
      project-owner-type: organization
      project-number: 5
      project-id: PVT_kwDOD6R9-s4Bbv4y
      config: |
        {
          "fields": {
            "Status": {
              "id": "PVTSSF_lADOD6R9-s4Bbv4yzhWeLVI",
              "type": "singleSelect",
              "options": {
                "Blocked": "c4c3f725",
                "Scoping": "37e901aa",
                "Todo": "f75ad846",
                "In Development": "47fc9ee4",
                "QA Testing": "8a2eac8e",
                "QA Passed": "e57d462f",
                "Test": "98236657",
                "Stage": "5f56e192",
                "Production": "98b2dac2",
              }
            }
          }
        }
